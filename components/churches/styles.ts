// Class strings specific to /churches. Shared page primitives (wrap, sheet,
// sectionPad, btnJade, lede, headingAccent) come from ../home/styles.

/** Section heading (the prototype's `.h2`), a touch tighter than the home displayHeading. */
export const churchHeading =
  "text-[clamp(40px,5.8vw,74px)] font-extrabold leading-[0.96] tracking-[-0.048em] text-zoe-ink";

/** Hairline outline used on white cards across the page. */
export const cardOutline = "outline outline-1 outline-[rgba(187,202,193,0.45)]";
